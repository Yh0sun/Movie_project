apiVersion: v1
kind: Pod
metadata:
  name: "centos"
  namespace: default
  labels:
    app: "centos"
spec:
  containers:
  - name: centos
    image: vega2k/centos:v1
    ports:
    - containerPort:  80